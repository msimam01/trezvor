# Environment Configuration

Create a `.env.local` file in the web directory with the following variable:

```env
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000/api/v1
```

For production, update this to your actual backend URL:

```env
NEXT_PUBLIC_BACKEND_URL=https://your-backend-api.com/api/v1
```